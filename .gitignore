node_modules/
*.log
*.pyc
__pycache__/
.pytest_cache/
.DS_Store
tsconfig.tsbuildinfo

# dist/ is intentionally committed: ppm installs the git tag as-is and does not
# run a build, and package.json "main" points into dist/.
